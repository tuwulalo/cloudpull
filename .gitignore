# Python
.venv/
__pycache__/
*.pyc

# Downloads and temp files
downloads/
*.part
*.ytdl

# Admin DB + analytics (contains admin password hash + 2FA secret)
data/

# Secrets
.env
.env.*
!.env.example
*.env.local
# Public production build values (no secrets) are committed:
!web/.env.production

# Node / Next.js (in case of running from the root)
web/node_modules/
web/.next/
web/out/

# Cloned third-party SEO skills (kept local, not part of this repo)
.claude/skills/
